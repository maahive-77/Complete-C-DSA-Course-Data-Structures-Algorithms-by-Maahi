// Description: This program prints the message "Hello, World!" to the standard output.
#include<iostream> // header file, include the iostream library to use the standard input/output stream objects
int main() // main function
{
    // std:: ye kyu use kiya? 
    // std:: is a namespace, which is a collection of identifiers (names) that are used to identify objects, functions, and types.
    std::cout << "Hello, World!"; // print the message "Hello, World!" to the standard output
    return 0; // return 0 to the operating system
}
